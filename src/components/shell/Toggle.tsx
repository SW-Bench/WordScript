// macOS-style toggle. The shadcn Switch already flips to the brand accent
// (--primary) when checked, which matches the WordScript design language.
export { Switch as Toggle } from "@/components/ui/switch";
